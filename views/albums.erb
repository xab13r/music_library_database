<!-- GET /albums -->

<html>
  <head></head>
  <body>
    <h1>Albums</h1>
    <% @albums.each do |album| %>
    <div>
      <a href="/albums/<%= album.id %>"><%= album.title %>, <%= album.release_year %></a>
    </div>
    <% end %>
  </body>
</html>